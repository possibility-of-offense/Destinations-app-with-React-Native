// React native
import { View, Text } from "react-native";

const LoginScreen = ({ navigation }) => {
  return (
    <View>
      <Text style={{ textAlign: "center", padding: 7, fontWeight: "bold" }}>
        Login (ABOUT TO BE IMPLEMENTED)
      </Text>
    </View>
  );
};

export default LoginScreen;
