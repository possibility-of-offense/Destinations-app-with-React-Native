// React native
import { StyleSheet } from "react-native";

// Colors
import colors from "../../config/colors";

export const primaryButtonStyles = StyleSheet.create({
  btn: { borderRadius: 22, padding: 9, overflow: "hidden" },
  text: {
    textAlign: "center",
  },
});
